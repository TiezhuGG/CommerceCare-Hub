# ADR-0001: Agent、工作流与领域写入边界

**状态：Accepted；日期：2026-07-30**

## 决策

Agent 输出仅为 schema 校验后的分类、事实请求、政策适用性、方案或客户文案。Workflow 编排步骤但不改变交易状态。所有状态变更和外部副作用只能由强类型 Domain Service 命令执行。

## 后果

增加了显式 command/schema 与编排代码，但使审批、幂等、测试和审计边界可验证，并防止模型直接写库。
