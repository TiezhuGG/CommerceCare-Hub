# ADR-0005: PII 最小化与不可信内容

**状态：Accepted；日期：2026-07-30**

## 决策

客户消息、附件、检索策略和所有 provider 输出均是不可信数据。日志与模型上下文采用最小化/脱敏字段；不保存完整敏感字段或私有推理。所有 UI/API 读取执行资源归属与 RBAC 双重授权。

## 后果

可降低数据泄露和提示词注入风险；需要 tokenization/redaction、明确的数据保留策略及安全回归测试。
